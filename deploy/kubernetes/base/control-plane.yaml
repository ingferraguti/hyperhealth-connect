apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane
spec:
  replicas: 2
  selector:
    matchLabels: {app.kubernetes.io/name: control-plane}
  template:
    metadata:
      labels: {app.kubernetes.io/name: control-plane, hhc.io/plane: control}
    spec:
      automountServiceAccountToken: false
      securityContext: {runAsNonRoot: true, seccompProfile: {type: RuntimeDefault}}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector: {matchLabels: {app.kubernetes.io/name: control-plane}}
      containers:
        - name: control-plane
          image: hhc/control-plane:0.1.0-phase0
          imagePullPolicy: Never
          ports: [{name: management, containerPort: 8080}]
          readinessProbe: {httpGet: {path: /actuator/health/readiness, port: management}, periodSeconds: 5}
          livenessProbe: {httpGet: {path: /actuator/health/liveness, port: management}, periodSeconds: 10}
          resources:
            requests: {cpu: 250m, memory: 256Mi}
            limits: {cpu: "1", memory: 768Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          volumeMounts: [{name: tmp, mountPath: /tmp}]
      volumes: [{name: tmp, emptyDir: {sizeLimit: 128Mi}}]
---
apiVersion: v1
kind: Service
metadata: {name: control-plane}
spec:
  selector: {app.kubernetes.io/name: control-plane}
  ports: [{name: management, port: 8080, targetPort: management}]

