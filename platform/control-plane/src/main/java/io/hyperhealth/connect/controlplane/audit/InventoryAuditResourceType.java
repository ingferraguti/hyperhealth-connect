package io.hyperhealth.connect.controlplane.audit;

/** Resource vocabulary deliberately narrower than arbitrary HTTP paths. */
public enum InventoryAuditResourceType {
    ENDPOINT,
    ENDPOINT_COLLECTION
}
